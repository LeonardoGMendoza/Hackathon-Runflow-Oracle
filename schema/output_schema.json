{
  "type": "object",
  "properties": {
    "temperatura": {
      "type": "string",
      "enum": ["hot", "warm", "cold"],
      "description": "A classificação térmica da oportunidade (em inglês)."
    },
    "score": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "description": "Pontuação de 0 a 100 indicando a propensão de fechamento."
    },
    "summary": {
      "type": "string",
      "description": "Dossiê completo da oportunidade, resumindo o histórico."
    },
    "sinais_criticos_detectados": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "sinal": { "type": "string", "description": "Qual foi o alerta ou sinal detectado" },
          "evidence": { "type": "string", "description": "CITAÇÃO EXATA: Trecho literal copiado da conversa." },
          "evidence_source": { "type": "string", "enum": ["e-mail", "WhatsApp", "reunião"], "description": "A fonte exata de onde a evidência foi tirada." }
        },
        "required": ["sinal", "evidence", "evidence_source"]
      }
    },
    "next_action": {
      "type": "string",
      "description": "Ação recomendada específica e executável na segunda-feira de manhã pelo vendedor."
    },
    "deal_value": {
      "type": "number",
      "description": "Valor financeiro (forecast) extraído das negociações. Deixe nulo se não houver citação."
    },
    "loss_reason": {
      "type": "string",
      "enum": ["preço", "concorrente", "timing", "sumiço"],
      "description": "Motivo da perda. APENAS preencher se a oportunidade estiver na etapa Perdido."
    }
  },
  "required": ["temperatura", "score", "summary", "sinais_criticos_detectados", "next_action"]
}
